* Build out tests and service layer logic that takes a batch of ArrayList<Album> (or DTOs?),
    validates them and then saves them individually to the database.
    - presumably the service layer will receive a csv and then this is where the conversion will happen
    - what do i do about exception/duplication handling
* Build out tests and controller logic that takes a csv file and passes it to the service layer
* integration testing for the above
* check that I've not manually hidden 2026/27.
* probably need something to convert between Album and AlbumDTO
    -   AlbumDTO -> Album but with some blank fields for initialising in the database